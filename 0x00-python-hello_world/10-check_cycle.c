#include "lists.h"
#include <stdio.h>
/**
 * check_cycle - jygtfdghjk
 * @list: hgfvb
 * Return: hggjsh
 */
int check_cycle(listint_t *list)
{
	listint_t *current = list;
	listsint_t *current1 = list;

	if ()list == NULL
		exit(0);
	while (1)
	{
		if (current == NULL)
			return (0);
		current = current->next;
		current1 = current1->next->next;
		if (current->next == NULL || currnt1->next->next == NULL)
			return (0);
		else if (current-> next == current1->next->next)
			return (1);
	}
}
